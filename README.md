# Area-Of-Cicrcle
The area of a circle in Java is calculated using the formula A = π × r × r, where r is the radius and π is a constant from the Math class as Math.PI. By taking the radius as input, the program multiplies it by itself and then by Math.PI to compute and display the circle’s area accurately.
